server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jinju
    username: root
    password: 930206
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource

  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: update
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true

mybatis:
  mapper-locations: classpath:mapper/*.xml

info:
  name: aguang
  age: 25
  height: 175